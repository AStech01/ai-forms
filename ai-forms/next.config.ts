// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    API_BASE_URL: 'http://your-api-base-url', // Replace with actual API URL
  },
};

module.exports = nextConfig;